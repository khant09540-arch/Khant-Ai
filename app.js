const $=s=>document.querySelector(s);document.querySelectorAll(".tabs button").forEach(b=>b.onclick=()=>{document.querySelectorAll(".tabs button").forEach(x=>x.classList.remove("active"));document.querySelectorAll(".panel").forEach(x=>x.classList.remove("active"));b.classList.add("active");$("#"+b.dataset.tab).classList.add("active")});$("#file").onchange=()=>{if($("#file").files[0])$("#preview").src=URL.createObjectURL($("#file").files[0])};$("#go").onclick=async()=>{const f=$("#file").files[0];if(!f)return $("#out").textContent="Video ရွေးပေးပါ။";const fd=new FormData();fd.append("video",f);$("#go").disabled=true;$("#out").textContent="⏳ OpenAI processing...";try{const r=await fetch("/api/translate-video",{method:"POST",body:fd});if(r.ok){const a=document.createElement("a");a.href=URL.createObjectURL(await r.blob());a.download="khant-ai-myanmar.mp4";a.click();$("#out").textContent="✅ ပြီးပါပြီ။"}else $("#out").textContent="❌ "+(await r.json()).error}catch(e){$("#out").textContent="❌ "+e.message}finally{$("#go").disabled=false}};$("#tts").onclick=async()=>{const text=$("#text").value.trim();if(!text)return $("#vout").textContent="စာထည့်ပါ";try{const r=await fetch("/api/voice",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({text})});if(r.ok){const a=document.createElement("a");a.href=URL.createObjectURL(await r.blob());a.download="khant-ai-voice.mp3";a.click();$("#vout").textContent="✅ MP3 ပြီးပါပြီ"}else $("#vout").textContent="❌ "+(await r.json()).error}catch(e){$("#vout").textContent="❌ "+e.message}};fetch("/api/health").then(r=>r.json()).then(x=>$("#status").textContent=x.openai?"● API Ready":"● Add API Key")